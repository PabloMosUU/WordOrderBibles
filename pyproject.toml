[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "wordorderbibles"
version = "0.1.0"
description = "Word order analysis for biblical texts"

[tool.setuptools.packages.find]
where = ["src"]
